#!/usr/bin/python3
def pow(a, b):
    value = 1
    for i in range(0, b):
        if b > 0:
            value *= a
        elif b < 0:
            value /= a
        elif b = 0
            return 1
    return value
